# WalletConnect 项目 ID（仅 RainbowKit 的 WalletConnect 弹窗需要；MetaMask 注入连接不需要）。
# Get one at https://cloud.reown.com — 留空也能用注入钱包。
VITE_WC_PROJECT_ID=

# 可选：覆盖合约地址（默认用 src/lib/contracts.ts 里已部署到 MegaETH 的地址）。
# VITE_ACCOUNT_IMPL=
# VITE_SESSION_VALIDATOR=
# VITE_WEBAUTHN_VALIDATOR=
# VITE_SPENDING_HOOK=
# VITE_EXAMPLE_EXECUTOR=
