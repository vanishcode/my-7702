# 可选：覆盖前端使用的预设测试私钥（默认值已内置在 web/src/lib/account.ts）。
# 请仅在本地测试环境使用，页面不会展示该值。
# VITE_PRESET_TEST_PK=0x...

# 可选：覆盖合约地址（默认用 src/lib/contracts.ts 里已部署到 MegaETH 的地址）。
# VITE_ACCOUNT_IMPL=
# VITE_SESSION_VALIDATOR=
# VITE_WEBAUTHN_VALIDATOR=
# VITE_SPENDING_HOOK=
# VITE_EXAMPLE_EXECUTOR=
# VITE_MULTISIG_VALIDATOR=
